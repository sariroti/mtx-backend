extends index.pug 

block content 

    h1(class="mt-3") Dashboard 
    

    div(class="col-md-10")
        h2(class="mt-3") Users
        table#user-list(class="table table-striped table-bordered")
            thead
                tr 
                    th(scope="col") name
                    th(scope="col") email
                    th(scope="col") total login attempt
                    th(scope="col") last login date

                    th(scope="col") signup date
            tbody 

block scripts 
    script(src="js/dashboard.js")